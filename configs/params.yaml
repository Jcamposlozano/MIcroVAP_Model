project:
  name: MicroNAV
  seed: 42

data:
  target: target_column
  raw_path: data/raw
  processed_path: data/processed

model:
  type: random_forest
  params:
    n_estimators: 200
    random_state: 42
